import 'package:flutter/material.dart';

class MyPhoneAuthPage extends StatelessWidget {
  const MyPhoneAuthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
